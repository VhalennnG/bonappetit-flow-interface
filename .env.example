# The URL of your deployed C++ Drogon backend (do not add a trailing slash).
# Leave empty for local development (defaults to local relative requests through Vite proxy).
# e.g., VITE_API_BASE_URL=https://your-backend-service-url.onrender.com
VITE_API_BASE_URL=
